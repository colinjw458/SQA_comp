import csp
from csp import ts
from datetime import datetime, timedelta

# Returns a variable x time ago (wip)
def delay(var, days):
    return var

# Returns the change in a variable x time ago (wip)
def delta(var, days):
    return var - delay(days)

